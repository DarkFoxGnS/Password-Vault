def generate(characterSet: str, length: int) -> str:
    import secrets
    # This must take in a regex of characters, and generate a string based on the input. The text must be generated using secrets and not using random for added security. The string the must be returned to the caller.

